{
  "name": "memorial-gallery",
  "private": true,
  "type": "module",
  "scripts": {
    "build:manifest": "node scripts/build-manifest.mjs",
    "optimize:images": "node scripts/optimize-images.mjs",
    "optimize:videos": "node scripts/optimize-videos.mjs",
    "optimize:all": "npm run optimize:images && npm run optimize:videos",
    "generate:favicon": "node scripts/generate-favicon.mjs",
    "dev": "bash scripts/setup-dev.sh && npx http-server _dev -p 8080 -c-1 -o",
    "start": "npm run dev",
    "check:size": "bash scripts/check-size.sh"
  },
  "dependencies": {
    "fast-glob": "^3.3.2",
    "sharp": "^0.33.2"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}

